.vercel
target/
.env
.env*.local
node_modules/
style/tailwindcss
.claude/*
!.claude/skills/
# Generated at build time by nextrs::build::sync_public_dir — mirrors
# site/public/ to the workspace root so Vercel's CDN picks it up.
/public/
# Generated at build time by nextrs::bundle::bundle_pages from page.tsx files.
# (react-todos ships its prebuilt bundle so it can deploy without a build-time
# bundler — see its rust-toolchain note and NEXTRS_SKIP_BUNDLE.)
site/public/dist/
