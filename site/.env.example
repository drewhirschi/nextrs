# Copy to `.env` for local dev. `cargo dev` (and the deploy binary) load it via dotenvy.

# Port for the local server. Defaults to 3000; if it's taken, the server now
# falls back to the next free port (up to +20) and logs which one it picked.
PORT=3000
