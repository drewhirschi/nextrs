# Build artifacts — regenerated by `npm run gen` (client) and `cargo build`
# (dist). The Vercel build regenerates both itself (see vercel.json
# buildCommand); committing them lets the server binary and the bundle skew
# across framework versions, which is exactly how the landing page broke.
client/src/generated/
public/dist/
.vercel
