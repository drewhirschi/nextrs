# hhh-next → nextrs migration time log

Data on how long each part of the conversion takes (per Drew: "good data on
how hard it is to convert nextjs to nextrs"). Wall-clock is elapsed real time;
agent-time notes whether work ran parallel to other tasks. Add a row when a
work unit starts; fill the end/duration when it lands.

| date | phase / slice | start | end | wall-clock | notes (what was hard) |
|---|---|---|---|---|---|
| 2026-06-11 | Phase 0: migration guide draft (subagent) | 17:29 | 17:40 | 11m | 1,091-line guide; read full framework source. Verified gaps: catch-all segments broken ({...x} → invalid Axum syntax), layout/loading.tsx not conventions, no error.tsx/metadata/cookie API, mode-1 typed props unimplemented |
| 2026-06-11 | Phase 0b: guide ↔ survey reconciliation (subagent) | 17:42 | 17:52 | 10m | guide 1,091→1,400 lines; new §6.2 server-action→shim pattern (same-signature TS shims, POST /api/actions/<module>/<fn>, serialization-drift hazards, per-endpoint authz hardening); auth re-ordered before API work; better-auth sidecar-vs-reimpl decision codified |
| 2026-06-11 | Phase 2 slice 1: scaffold (subagent) | 17:44 | 18:00 | 16m | branch builds + serves hydrated /about with full shadcn CSS. 5 commits. Gotchas: lockfile drift hazard hit exactly as guide warned (seeded Cargo.lock from example); NEXTRS_SKIP_BUNDLE=0 needed locally since app dir IS the cargo root; static layout.html uses {{ children }} not {{ children\|safe }} |
| 2026-06-11 | Framework: catch-all [...x] → {*x} fix (main session) | 17:53 | 17:57 | 4m | discovery.rs + macro url_from_file + tests (61 green); published 0.2.0 lacks it — conversion uses enumeration or git dep until 0.2.1 |
| 2026-06-11 | Phase 2 slice 2: auth (subagent) | 18:00 | 18:40 | 40m (incl. 11m lost to a stalled first attempt) | Sidecar architecture per guide §10(a): Bun sidecar :3100 + 8 enumerated forwarder routes (catch-all not in published 0.2.0; middleware can't forward — only runs on matched routes). Rust session layer validates HMAC-signed cookie against session table (raw token in DB, sig = b64 HMAC-SHA256). Full proxy.ts guard matrix + /, /auth/login, /auth/register converted; browser-verified incl. expired/tampered-cookie rejection. Found framework bug: BundleConfig @/* alias doesn't glob in rolldown (worked around via root tsconfig paths). Gotcha: session.expires_at is timestamptz-less local wall time |
| 2026-06-11 | Framework: @/* alias glob bug fix (main session) | 18:48 | 18:58 | 10m | bundle.rs::build_aliases — resolver alias keys are prefix matches, `*` never globbed, built-in @/* was dead code; normalized X/* → X/ prefix form + user-overrides-builtin; 4 new tests, 91 green. Found by auth slice |
| 2026-06-11 | Phase 2 slice 3: domain/query layer + engines (subagent) | 18:46 | 19:31 | 45m | src/domain/ 15 files; 93 Rust tests green incl. 45 engine tests ported from bun suite (332 green on baseline first). Byte-level wire parity on 4 representative fns. Found: real overbooking race (FOR UPDATE + count-subquery in one stmt under READ COMMITTED), bun suite only green under TZ=UTC pin, credit-FIFO had no bun coverage. 6 deliberate transactional fixes documented |
| 2026-06-11 | Phase 2 slices 4a-4c: action modules → route.rs + shims (3 parallel subagents, worktrees) | 19:34 | 20:15 | 41m wall (~80m agent-time across 3) | All 68 server actions → POST endpoints + same-signature shims. 4a 19 eps (owns _rpc.ts; fixed guide's date-reviver regex), 4b 23 eps (found+fixed integral-NUMERIC drift), 4c 26 eps (sidecar-backed createUserAndInvite; final message lost to API error but all 3 commits + notes landed). Merge: one add/add conflict (src/actions.rs, 4a superset won); 4c was a fast-forward. 93 tests green post-merge |
| 2026-06-11 | Phase 2 slices 5a/5b/5c: pages (user+public / admin+RSC) + avatar API (3 parallel subagents) | 20:18 | 21:03 | 45m | 5a: contact/pricing byte-identical + 4 user-section pages. 5b: 13 admin pages + /404, 2 RSC pages → seeded client pages + 3 GET twins + orval regen. 5c: avatar GET/POST/DELETE (aws-sdk-s3 + image crate, live parity vs Next) |
| 2026-06-11 | Phase 2 integration: merge 5a/5b/5c + artifact regen (main session) | 21:03 | 21:08 | 5m | conflicts only in union-mergeable/generated files (tsconfig include, package.json deps, bun.lock, style.css). Full release build: 24 page bundles, 98 tests green, tsc clean. CONVERSION CODE-COMPLETE |
| 2026-06-11 | Phase 3: full parity verification (subagent) | 21:08 | 22:02 | 54m | PASS: 24 routes × 3 personas × 2 apps; money flows step-identical (quirks preserved: no-refund-with-active-sub, UTC date display, week-shift TZ quirk). Wire audit 14 endpoints: found+fixed integral ::float drift (240.0 vs 240) and sqlx zero-NUMERIC scale loss ("0" vs "0.00"). 404 parity layer pulled forward. Not verified: Stripe/Google OAuth/SMTP (no creds), dnd drag (tooling) |

**Phase 2+3 total: scaffold → verified conversion in ~4.5h wall clock (~6h agent-time), 2026-06-11 17:44–22:02.**

| date | phase / slice | start | end | wall-clock | notes (what was hard) |
|---|---|---|---|---|---|
| 2026-06-12 | Phase 4: local big-app benchmark (main session) | 08:25 | 08:40 | 15m | bench-hhh-local.sh: / 522×, authed /app 99× (DB-bound both sides), RSS 91.7 vs 235.8 MB (nextrs RSS grew 16× vs todo app; Node ≈flat — runtime floor) |
| 2026-06-12 | Phase 4: deployed setup (main session) | 08:20 | 08:52 | 32m | hhh-bench-next: clean. hhh-nextrs: 5-failure saga — 22k-file upload (target/ → .vercelignore), framework auto-detection ran next build (framework=null pin), >100MB style/tailwindcss binary, then project stuck in BLOCKED (no reason via API; flag was project-scoped). Fix: local `vercel build` (needs zig + cargo-zigbuild for the Lambda-glibc cross-compile) + `deploy --prebuilt` to a FRESH project (hhh-rs) → Ready in 16s. All gotchas codified in guide §11 |
| 2026-06-12 | Phase 4: deployed cold-start suite on big apps (background) | 08:48 | 09:19 | 31m | Latency: **nextrs cold p50 215ms ≈ its warm (209ms); Next.js cold p50 4,323ms** — the realistic-deps blowout the methodology predicted (todo-app gap was 648 vs 830). Frequency at 150 workers: **nextrs 43 instances vs Next.js 100** (57% fewer), cold/1k halved, Next p95 TTFB 5.5s. PHASE 4 COMPLETE |

**Project total (conversion + verification + full benchmark suite): 2026-06-11 17:29 → 2026-06-12 09:19, ~6h working wall-clock across two sessions.**

| date | phase / slice | start | end | wall-clock | notes (what was hard) |
|---|---|---|---|---|---|
| 2026-06-12 | Slice 6: replace Bun auth sidecar with native Rust better-auth port (subagent) | 09:32 | 10:33 | 61m | 48/48 oracle diffs vs live sidecar before deletion; 111 tests incl. scrypt golden vector (salt = ASCII-of-hex quirk, NFKC), signed state cookie + S256 PKCE, mocked-OAuth callback machine. Corrected a slice-2 fixture misreading (postgres-js writes UTC, reads node-local — wire timestamps skewed +6h on dev, identical in prod). Deployed app is now a single Rust binary, zero Node functions |
| 2026-06-12 | Slice 6 deploy: BLOCKED mystery solved + single-binary live (main session) | 10:35 | 11:40 | 65m (mostly blocked-state diagnosis + waiting on Drew) | Vercel's reason-less BLOCKED state = commit author email (drew@enzo.health) not matched to a GitHub account. Fixed by rewriting be4d82e..nextrs (45 commits) + bench branch to ashirsc@gmail.com (git filter-branch) + repo-local user.email. Redeployed Ready in 11s; deployed cold p50 re-verified at 214ms with native auth in the binary |
| 2026-06-12 | Slice 7: prod-schema enum drift fix (subagent) | 13:11 | | | Deployed login 500/signup 422: prod schema is Prisma-born (native PG enums: Role, Status, PaymentMethod, Type, BookingLimitType, BookingLimit, BillingType) but repo migrations recreated them as TEXT for local dev — postgres-js tolerated it, sqlx doesn't. Found via Drew's dashboard log paste (vercel logs CLI streamed nothing). Affects domain layer too, not just auth |
| 2026-06-11 | Phase 1: hhh-next survey (subagent) | 17:29 | 17:37 | 8m | read-only inventory, parallel with Phase 0. Headline: backend is 68 server actions across 12 modules, not REST routes; 23 pages (18 already client); 15 tables; better-auth + timezone semantics + booking/credit engine flagged riskiest |
