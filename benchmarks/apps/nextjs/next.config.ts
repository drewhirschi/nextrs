import type { NextConfig } from "next";

// Defaults — App Router, Node serverless functions. Nothing tuned away; this
// is a stock idiomatic Next.js 15 config so the comparison is against Next's
// normal output, not a hand-stripped build.
const nextConfig: NextConfig = {};

export default nextConfig;
