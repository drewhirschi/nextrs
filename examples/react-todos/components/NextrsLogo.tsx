// Shared application components can live at the project root. They are normal
// source code, separate from the generated package hidden under `.nextrs/`.
export function NextrsLogo() {
  return (
    <span aria-label="nextrs">
      next<b>rs</b>
    </span>
  );
}
