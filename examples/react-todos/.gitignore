.vercel
# Build artifacts — regenerated by `npm run gen` (client) and `cargo build`
# (dist; the framework bundles page.tsx and rewrites src/generated/index.ts).
# The Vercel build regenerates both itself (see vercel.json buildCommand).
client/src/generated/
client/dist/
public/dist/
Cargo.lock
